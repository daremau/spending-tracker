services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "3000"
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@pgbouncer:5432/spending_tracker?schema=public
    depends_on:
      pgbouncer:
        condition: service_started
    restart: unless-stopped
    networks:
      - traefik-net
      - internal
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-net"
      - "traefik.http.routers.spending-tracker.rule=PathPrefix(`/`)"
      - "traefik.http.routers.spending-tracker.entrypoints=web"
      - "traefik.http.services.spending-tracker.loadbalancer.server.port=3000"

  db:
    image: postgres:16-alpine
    ports:
      - "5434:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=spending_tracker
      - POSTGRES_HOST_AUTH_METHOD=trust
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - internal

  # PgBouncer connection pooling
  pgbouncer:
    image: edoburu/pgbouncer:latest
    ports:
      - "5432:5432"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/spending_tracker
      - POOL_MODE=transaction
      - MAX_CLIENT_CONN=1000
      - DEFAULT_POOL_SIZE=20
      - MIN_POOL_SIZE=5
      - RESERVE_POOL_SIZE=5
      - IGNORE_STARTUP_PARAMETERS=search_path
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - internal

  # Run migrations on startup (one-time job)
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
    command: npx prisma@5.22.0 db push --skip-generate
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@pgbouncer:5432/spending_tracker?schema=public
    depends_on:
      pgbouncer:
        condition: service_started
    networks:
      - internal

volumes:
  postgres_data:

networks:
  traefik-net:
    external: true
  internal:
